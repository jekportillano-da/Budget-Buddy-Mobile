# Production Environment Variables Template for Railway
# Copy these to Railway Dashboard → Your Project → Variables

# =============================================================================
# REQUIRED PRODUCTION VARIABLES
# =============================================================================

# JWT Secret Key (CRITICAL - Generate with: openssl rand -hex 32)
SECRET_KEY=your_secure_jwt_secret_key_here

# Cohere AI API Key (Required for AI features)
COHERE_API_KEY=your_cohere_api_key_here

# Database URL (Automatically provided by Railway PostgreSQL)
DATABASE_URL=postgresql://user:pass@host:port/dbname

# =============================================================================
# OPTIONAL PRODUCTION VARIABLES
# =============================================================================

# OpenAI API Key (Optional, for additional AI features)
OPENAI_API_KEY=your_openai_api_key_here

# Environment indicator
ENVIRONMENT=production

# Server configuration
PORT=8000

# CORS Origins (comma-separated, Railway domain will be auto-added)
ALLOWED_ORIGINS=https://budgetbuddy.app,https://www.budgetbuddy.app

# =============================================================================
# RAILWAY AUTO-PROVIDED VARIABLES (Don't manually set these)
# =============================================================================

# RAILWAY_ENVIRONMENT=production (auto-set)
# RAILWAY_STATIC_URL=your-app-name.railway.app (auto-set)
# DATABASE_URL=postgresql://... (auto-set when you add PostgreSQL service)

# =============================================================================
# FRONTEND VARIABLES (Update in your React Native .env.local)
# =============================================================================

# EXPO_PUBLIC_BACKEND_URL=https://your-app-name.railway.app
# EXPO_PUBLIC_API_MODE=backend
# EXPO_PUBLIC_ENABLE_HYBRID_AI=true
# EXPO_PUBLIC_ENABLE_BACKEND_AUTH=true

# =============================================================================
# SECURITY NOTES FOR PRODUCTION
# =============================================================================

# 🔐 SECRET_KEY:
# - Generate with: openssl rand -hex 32
# - Must be at least 32 characters
# - Keep secret and rotate regularly

# 🔑 API KEYS:
# - Use production API keys, not development
# - Monitor usage and set rate limits
# - Rotate keys if compromised

# 🛡️ DATABASE:
# - Railway PostgreSQL includes SSL by default
# - Connection pooling handled automatically
# - Backups included in Railway Pro plan

# 🌐 CORS:
# - Add your production domains
# - React Native requires "*" or specific expo:// URLs
# - Remove localhost origins in production

# 📊 MONITORING:
# - Railway provides metrics and logs
# - Consider adding Sentry for error tracking
# - Monitor API rate limits and costs